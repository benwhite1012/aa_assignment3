#ifndef MEDIAN_HPP_
#define MEDIAN_HPP_

#include <string>
#include <vector>

int qselect(std::vector<int>& vec, std::string p_method);
int mom(std::vector<int>& vec, std::string p_method);

#endif      // MEDIAN_HPP_