// median.cpp
#include <string>
#include <vector>
#include <iostream>
#include "median.hpp"

//quick select
int qselect(std::vector<int>& vec) {
    std::ignore = vec;
    return -1; //placeholder
}

//median of medians
int mom(std::vector<int>& vec) {
    std::ignore = vec;
    return -1; //placeholder
}